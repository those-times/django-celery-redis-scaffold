# django-celery-redis-scaffold

Dockerized project for Django using celery on redis.


After launching docker-compose, go to    
`http://localhost:8000`

copy taskId, and paste it into url    
`http://localhost:8000/<taskId>/`
